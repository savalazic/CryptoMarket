import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  Header: {
    fontSize: 35,
    textAlign: 'center',
    fontWeight: '600',
    marginBottom: 20,
  },
  Input: {
    marginBottom: 15,
  },
});
